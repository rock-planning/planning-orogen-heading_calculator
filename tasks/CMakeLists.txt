# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(heading_calculatorTaskLib)
ADD_LIBRARY(${HEADING_CALCULATOR_TASKLIB_NAME} SHARED 
    ${HEADING_CALCULATOR_TASKLIB_SOURCES})
add_dependencies(${HEADING_CALCULATOR_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${HEADING_CALCULATOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${HEADING_CALCULATOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${HEADING_CALCULATOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${HEADING_CALCULATOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${HEADING_CALCULATOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${HEADING_CALCULATOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/heading_calculator)

